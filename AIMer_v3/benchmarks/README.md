# Benchmarks

Benchmark programs and result files for AIMer v3 belong here. Measurements
should always report the parameter set, implementation (`ref` or `opt`), CPU,
compiler, compiler flags, and whether AVX-512 Keccak was enabled.

Add performance measurements only after the corresponding implementation passes
the official KAT.
